spring:
  threads:
    virtual:
      enabled: true
  data:
    redis:
      lettuce:
        pool:
          min-idle: 8
          max-idle: 16
          max-active: 32
      host: localhost
      port: 6379